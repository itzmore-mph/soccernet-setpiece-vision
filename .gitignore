# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# Jupyter
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.kiro/
.CLAUDE.md
.claude/

# Conda / venv (env lives in py311-dev outside repo)
.venv/
venv/

# Data: never commit. Lives on external SSD.
/Volumes/
data/raw/
data/interim/
example/

# Large model weights (auto-downloaded to ~/.cache)
*.pt
*.onnx

# Outputs: keep parquet + figures (small, reproducible-from-notebooks)
# Diagnostic per-clip videos excluded (33 clips each, ~GB total).
# The 2 representative video_*.mp4 files are committed intentionally.
outputs/figures/annotated/
outputs/figures/overlay/
outputs/figures/*.mp4
!outputs/figures/video_*.mp4
report.pdf
report.docx
